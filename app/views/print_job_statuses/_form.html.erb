<%= form_for(@print_job_status) do |f| %>
  <% if @print_job_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@print_job_status.errors.count, "error") %> prohibited this print_job_status from being saved:</h2>

      <ul>
      <% @print_job_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
