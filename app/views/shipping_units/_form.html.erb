<%= form_for(@shipping_unit) do |f| %>
  <% if @shipping_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shipping_unit.errors.count, "error") %> prohibited this shipping_unit from being saved:</h2>

      <ul>
      <% @shipping_unit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
