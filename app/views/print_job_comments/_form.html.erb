<%= form_for(@print_job_comment) do |f| %>
  <% if @print_job_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@print_job_comment.errors.count, "error") %> prohibited this print_job_comment from being saved:</h2>

      <ul>
      <% @print_job_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
