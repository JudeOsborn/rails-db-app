<%= form_for(@items_ordered) do |f| %>
  <% if @items_ordered.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@items_ordered.errors.count, "error") %> prohibited this items_ordered from being saved:</h2>

      <ul>
      <% @items_ordered.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
